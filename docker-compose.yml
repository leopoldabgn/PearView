services:
  pearview:
    build: .
    container_name: pearview
    ports:
      - "1500:1500"
    volumes:
      - ./data:/app/data
    environment:
      - PEARVIEW_PORT=1500
      - PEARVIEW_HOST=0.0.0.0
    restart: unless-stopped